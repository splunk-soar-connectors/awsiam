**Unreleased**
* Escape AWS IAM widget values before embedding them in JavaScript contexts.
* Revoke older temporary sessions and clarify remaining credential scope when disabling IAM users.
* Prevent console passwords and temporary AWS credentials from being stored in action results.
* Bound IAM pagination and reject responses that do not advance their marker.
